# Legacy Fig.3 runs

Unregistered experiments migrated from the old `outputs/conditional/fig3_*` layout.

Each subfolder has `outputs/` (local, gitignored). These are kept for reference — random split, seed sweeps, early ablations — not the primary official-split benchmark in `experiments/fig3/INDEX.csv`.
